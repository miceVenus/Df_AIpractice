# Text Processing Application

这是一个简单的文本处理应用程序，使用PyQt5构建图形用户界面（GUI），并使用MVC（Model-View-Controller）模式组织代码。

## 运行

在终端中导航到项目目录并运行以下命令：

```bash
python main.py
pip install -r requirements.txt
修改说明
在 model.py 中的 processTextRank 和 processTextMt5 方法中实现具体的文本处理逻辑。你需要调用后端API来处理文本，并返回处理后的字符串。

示例：

class Model:
    def process_text_rank(self, textIn, stopFlag, dampingFactor, excertLength, algorithm):
        # 调用后端API
        text = "调用TextRank API后的结果"
        return text
    
    def process_text_mt5(self, textIn, stopFlag, dampingFactor, excertLength, algorithm):
        # 调用后端API
        text = "调用Mt5 API后的结果"
        return text

配置环境
使用Python 3.10.10环境，并安装所需的依赖：
    pip install -r requirements.txt